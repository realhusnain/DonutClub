<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.firstpage">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/full_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Firstpage"
        tools:targetApi="31">
        <activity
            android:name=".activities.MainActivityChat"
            android:exported="true">


        </activity>

        <activity
            android:name=".activities.New_Screen"
            android:exported="false" /> <!-- android:parentActivityName=".New_Screen" -->
        <activity
            android:name=".activities.HomeActivity"
            android:exported="true"
            android:label="Galaxy Donut Club" />
        <activity
            android:name=".activities.MainActivity2"
            android:exported="true"
            android:label="Welcome to Sign-up Screen"
            android:parentActivityName=".activities.MainActivity" />
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>


        </activity>
    </application>

</manifest>